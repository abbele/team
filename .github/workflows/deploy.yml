# Questo workflow è disabilitato — il deploy è gestito da nextjs.yml
name: Deploy (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use nextjs.yml for deployments"
